

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carproject
    username: car
    password: Sa123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: false

  # ✅ Mail config for sending verification codes
  mail:
    host: smtp.gmail.com
    port: 587
    username: mengricky200@gmail.com       # ← your Gmail
    password: pfts xmeq wyae npwx        # ← App Password (not regular password)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

telegram:
  bot:
    token: "8545203039:AAFRJD8BNuXb9hjxe3ALh89qq0C5ZWCcot0"
  chat:
    id: "1551363178"